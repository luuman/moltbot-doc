---
summary: "`moltbot dashboard` 的 CLI 参考（打开控制 UI）"
read_when:
  - 您想使用当前令牌打开控制 UI
  - 您想打印 URL 而不启动浏览器
---

# `moltbot dashboard`

使用当前身份验证打开控制 UI。

```bash
moltbot dashboard
moltbot dashboard --no-open
```

